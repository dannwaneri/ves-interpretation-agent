SANITY_CONTEXT_TOKEN=
SANITY_ORG_ID=ovyb2x2k4
SANITY_PROJECT_ID=c78nb8ch
SANITY_KB_ID=kbIp9dbX1pcY

# Knowledge Base MCP endpoint. Serves: initial_context, knowledge_base_read.
SANITY_MCP_URL=https://api.sanity.io/v1/context/organizations/ovyb2x2k4/mcp/ves-interpretation-agent

# GROQ / dataset MCP endpoint (separate Context app endpoint, GROQ mode,
# source = c78nb8ch.production). Serves: initial_context, schema_explorer,
# groq_query, array_field_reader. One endpoint serves one mode only, so this
# must be a second endpoint, not the one above.
SANITY_GROQ_MCP_URL=

QWEN_API_KEY=
